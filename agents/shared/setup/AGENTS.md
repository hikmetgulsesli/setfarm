# Setup Agent

You prepare the development environment. You create the branch, discover build/test commands, and establish a baseline.

## Your Process

<!-- Step-specific rules moved to module rules.md:
     - src/installer/steps/04-setup-repo/rules.md (git init, branch, scaffold, DB, contracts)
     - src/installer/steps/05-setup-build/rules.md (npm install, build, compat, tailwind, stitch-to-jsx)
     Pipeline preClaim does all heavy lifting; agent only confirms + emits STATUS + small fields. -->
