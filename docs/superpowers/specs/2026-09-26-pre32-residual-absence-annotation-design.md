# Bounded residual absence annotation before Task 6A

The exact-journal V7 held pair and V2 prunable-record annotation leave six physical blockers on the current host: four discovered directories without a `.git` marker, one absent workflow `agents` discovery parent, and one selected historical worktree Git-admin mutation. The physical producer already checks a non-Git marker and absent parent in both passes while directory descriptors are held. Its existing result does not separately expose those five bounded observations. None proves continuous absence after descriptor release, nonownership, or cutover readiness.

Add a separate V3 diagnostic projection over a newly observed V2 annotation. It must not edit the V7 pair, V2 annotation, physical catalog, migration journal, or owner census. It partitions only V2's remaining blockers into `boundedAbsenceBlockers` and `otherResidualBlockers`, retains the entire source annotation and original blockers byte-for-byte, and hashes the new wrapper. A `non-git-child` may be bounded only if exactly one matching physical entry is unresolved, has no Git primary or dirty state, and has no referencing PID. An `absent-workflow-agents-discovery-parent` may be bounded only at an exact workflow `/agents` discovery path with no physical entry at or below it. Duplicated candidate blockers or entries refuse, not silently deduplicate. All other reasons, especially Git-admin churn, stay residual. This is a bounded two-pass fact about the V7 interval, not a trusted positive/negative owner classification.

The wrapper carries literal `authority:diagnostic-only`, `physicalIdentityProvenance:unverified`, `temporalScope:v7-held-two-pass`, the V2 annotation by identity, arrays of the same original blocker objects, and a canonical hash. It grants no zero-owner lease, admission, producer, migration, or service action. A future continuous exclusion guard may use this as an input only after revalidating exact source and adding post-return writer/process control; V3 itself never subtracts original physical blockers from the cutover gate.

Task 6A currently requires exact guarded migration 32 as its sole pending successor, then ordinary 33. A separate ordinary-execution worktree receipt journal must not be registered as migration 34 before Task 6A, and A–E manifest rows remain frozen. If Task 6A cannot prove physical zero-owner while preserving visible retained worktrees, it remains fail-closed and requires a separately reviewed protocol revision. V3 does not solve that sequencing condition.

## File Map

- `src/internal-production/baseline-positive-worktree-pre32-residual-absence-annotation-v3.ts`: new bounded partition and V2-composed diagnostic observer, with no I/O of its own.
- `tests/internal-production/baseline-positive-worktree-pre32-residual-absence-annotation-v3.test.ts`: RED/GREEN exact partition, malformed source and PID/duplicate negatives.
- `package.json`: include the new pure test in the existing internal-production pure suite.
- `docs/superpowers/plans/2026-09-26-pre32-residual-absence-annotation.md`: execution and verification record.
