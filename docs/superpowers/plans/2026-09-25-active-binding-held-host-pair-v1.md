# Active binding held host pair V1

Status: locally verified; PR delivery pending. Diagnostic-only; no cutover
authority.

## Causal scope

PR #168 supplied an import-inert cutover-local URL observer, but no authenticated
host path invokes it. A separate positive active/binding physical pair is needed
to test actual active rows against a physical bracket without using the pre-32
zero census. It must keep V4/V5/V6 unchanged and preserve every physical
blocker, incomplete/non-Git root and unknown source-provenance state.

## File map / RED-GREEN

1. `tests/internal-production/baseline-positive-worktree-host-pair-v2.test.ts`:
   RED for missing positive pair port; positive linked attempt/session test,
   crossed counts/identities and self-rehashed malformed nested snapshots;
   single held DB callback and preserved physical blockers.
2. `src/internal-production/baseline-positive-worktree-host-pair-v2.ts`:
   versioned positive pair and zero-input code-owned composition, validating
   strict inner hashes, counts and overlapping identities, with held launcher
   cleanup and finite refusal. Reuse the physical V2 two-pass bracket.
3. `src/internal-production/baseline-deployment-cutover-launcher-observation-v1.ts`:
   private `activeBindingSnapshot` port through the existing agreed two-plist
   URL and qualified/idle/rechecked database observer. No URL in result.
4. `scripts/deployment-cutover.mjs` and script tests: distinct authenticated
   `inspect-active-binding-host-pair-v1 --json` with exact diagnostic schema,
   hash and sanitized refusal. V4/V5/V6 output untouched.
5. Focused, pure, cutover, script, TypeScript and contracts; independent
   read-only review; PR; clean-main build and authenticated real-host check.

No owner/nonowner classification, service switch, migration, destructive
cleanup, selected historical dist replacement or runtime guard bypass.

## Verification evidence

RED failed on the missing positive pair export, private launcher port and
bootstrap verb. Focused GREEN demonstrated positive active attempt/session
rows, one held physical callback, preserved blockers, crossed identity/count
and nested-hash refusal, private qualified URL, zero-input launcher cleanup and
sanitized bootstrap. Pure suite 146/146, cutover suite 391/391, script suite
119/119, TypeScript no-emit, English/path and diff checks passed. Independent
read-only review found no blocker and requested extra negative active-list/hash
and self-consistent cutover-authority tests; both passed after the broad suites.
No code changed after those broad suites. PR review, clean-main build and real
host proof remain pending.
